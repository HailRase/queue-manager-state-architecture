/**
 * Purpose: Public store entry — entity map + hydration.
 * Inputs: none.
 * Outputs: `useEntityStore`, types.
 */
export {
  useEntityStore,
  type EntityStore,
  type EntityHydrationStatus,
} from './entityStore';
